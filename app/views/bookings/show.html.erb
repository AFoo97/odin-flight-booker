<h1>Booking Confirmation</h1>
<p>Flight: <%= @booking.flight.departure_airport.code %> -> <%= @booking.flight.arrival_airport.code %></p>
<p>Date: <%= @booking.flight.start_datetime.strftime("%b %d %H:%M") %></p>

<h2>Passengers</h2>
<ul>
  <% @booking.passengers.each do |p| %>
    <li><%= "#{p.name} (#{p.email})" %></li>
  <% end %>
</ul>

